import React, { memo } from 'react'
import { Text, View } from 'react-native'

// TODO: Setup and UI
const ReviewTransferScreen = () => {
    return (
        <View>
            <Text>{'Review Transfer Screen'}</Text>
        </View>
    )
}

export default memo(ReviewTransferScreen)
